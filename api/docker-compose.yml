services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aidlc_auth
      POSTGRES_USER: aidlc
      POSTGRES_PASSWORD: aidlc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
